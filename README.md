# url-template-by-header-customizer

Simple exemple to evaluate the open telemety java agent extension on loading HttpClientUrlTemplateCustomizer

The run.sh script is provided to build and run the application with the OTEL java agent with simple http request to touch the functionality explored.

